<UserControl x:Class="WpfControlLibrary1.UserControl_Login"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfControlLibrary1"
             mc:Ignorable="d"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Width="350"
             Padding="24">
    <StackPanel>
        <materialDesign:PackIcon Kind="AccountCircle"
                                 Width="60"
                                 Height="60"
                                 HorizontalAlignment="Center"
                                 Foreground="{DynamicResource MaterialDesignPrimaryBrush}"
                                 Margin="0,0,0,16" />
        <TextBlock Text="系統登入"
                   FontSize="24"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,24" />

        <TextBox Text="{Binding Account}"
                 materialDesign:HintAssist.Hint="員工編號"
                 Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                 Margin="0,0,0,16" />
        <TextBox Text="{Binding Password}"
                 materialDesign:HintAssist.Hint="密碼"
                 Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                 Margin="0,0,0,8" />

        <TextBlock Text="{Binding ErrorMessage}"
                   Foreground="Red"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,16"
                   Height="20" />

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button Command="{Binding CancelCommand}"
                    Content="取消"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Margin="0,0,8,0" />
            <Button Command="{Binding ExecuteLoginCommand}"
                    Content="登入"
                    Grid.Column="1"
                    Margin="8,0,0,0" />
        </Grid>
    </StackPanel>
</UserControl>
