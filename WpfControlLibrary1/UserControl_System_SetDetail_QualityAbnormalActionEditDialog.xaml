<UserControl x:Class="WpfControlLibrary1.UserControl_System_SetDetail_QualityAbnormalActionEditDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Width="400"
             Padding="24"
             Background="{DynamicResource MaterialDesignPaper}">
    <StackPanel>
        <TextBlock Text="{Binding DialogTitle}"
                   FontSize="20"
                   FontWeight="Bold"
                   Foreground="{DynamicResource MaterialDesignBody}"
                   Margin="0,0,0,24"
                   HorizontalAlignment="Center" />

        <TextBox Text="{Binding BarCode, UpdateSourceTrigger=PropertyChanged}"
                 materialDesign:HintAssist.Hint="異常代碼 (主鍵)"
                 Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                 IsReadOnly="{Binding IsEditMode}"
                 FontSize="16"
                 Margin="0,0,0,16" />

        <TextBox Text="{Binding Definition, UpdateSourceTrigger=PropertyChanged}"
                 materialDesign:HintAssist.Hint="異常處理動作"
                 Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                 FontSize="16"
                 Margin="0,0,0,32" />

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button Command="{Binding CancelCommand}"
                    Content="取消"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Height="40"
                    FontSize="14"
                    Margin="0,0,8,0" />
            <Button Command="{Binding ConfirmSaveCommand}"
                    Content="儲存變更"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Grid.Column="1"
                    Height="40"
                    FontSize="14"
                    Margin="8,0,0,0" />
        </Grid>
    </StackPanel>
</UserControl>